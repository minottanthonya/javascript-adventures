// Write a doMath function that takes three parameters called operator / numOne / numTwo. 
// The function should then return correct calculations based upon the arguments.

// Your function should handle add, subtract, multiply, divide and mod(modulus)
// If an unknown operator is used it should return Operator not available
